🎯 **What:** Added unit tests for the `clampNonNegativeIntInput` function in `app.js`.
📊 **Coverage:** Tested `null`/`undefined` inputs, empty/non-digit strings with and without the `fillEmpty` flag, stripping of non-digits, stripping of leading zeros, and handling of a missing `value` property on the input mock.
✨ **Result:** Improved test coverage for DOM input manipulation edge cases, making future refactoring safer.
