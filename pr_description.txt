Title: 🧹 Consolidate repeated Modals close setup code

Description:
🎯 **What:** Abstracted the repetitive logic for opening and closing modals (Mobile Menu Drawer, Cart Sidebar, Preview Modal, and Success Modal) into a single, generic `setupModal` function in `app.js`.

💡 **Why:** By reducing duplication, the codebase becomes more readable, easier to maintain, and less prone to errors when implementing new modals in the future.

✅ **Verification:**
- Verified that all modal-related event listeners are successfully replaced.
- Confirmed that functionality correctly handles `if (e.target === overlay)` for overlay closing, ensuring clicking inside the modal content doesn't abruptly close it (e.g. for `previewModal`).
- Verified JavaScript syntax using `node -c app.js`.
- Confirmed changes passed automated code review.

✨ **Result:** Cleaned up ~40 lines of duplicated modal setup code, replacing it with a single, highly reusable function while maintaining 100% of the original functionality.
